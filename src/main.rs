extern crate tui;
extern crate termion;
#[allow(dead_code)]

use std::io;
use tui::Frame;
use tui::layout;
use tui::widgets;
use tui::buffer;
use tui::backend::TermionBackend;
use termion::*;


fn main() {
    println!("test")
}

#[test]
fn render() {
   // let window = termion::;
}

